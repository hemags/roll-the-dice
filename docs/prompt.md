I need a simple, self‑contained web app that rolls a virtual dice and displays the result.

Requirements:

It should be a single HTML file that includes the HTML, CSS, and JavaScript inline (no external files).

There should be a button labeled “Roll Dice” that, when clicked, generates a random number between 1 and 6 and displays it on the page.

The displayed result should be clearly visible (e.g., in a large font or a box).

Optionally, you can add a small animation or visual effect when the dice is rolled (like changing the button color or briefly showing “Rolling…”).

The design should be minimal and modern, using light colors and centered content.

Deliverables:

Provide the complete HTML code in one code block.

Do not include build tools, frameworks, or external libraries (e.g., no React, no jQuery).

Add a brief comment at the top explaining how to use the app (just open the HTML file in a browser).